# 复制本文件为 keystore.properties 并填入你的签名信息。
# keystore.properties 已在 .gitignore 中，不会提交。
# 缺少 keystore.properties 时会回退到 debug 签名（可安装调试，但不能正式发布）。
# storeFile 可写绝对路径，也可写相对 android/ 的路径。
storeFile=C:/path/to/lucimatools.keystore
storePassword=你的keystore密码
keyAlias=你的key别名
keyPassword=你的key密码
